- name: Install yum packages
  yum:
    name: '{{ item }}'
    state: present
  with_items:
    - wget
    - git
    - tar
    - bind-utils
    - sysstat
    - zip

- name: stop firewalld
  service:
    name: firewalld
    state: stopped
    enabled: no
