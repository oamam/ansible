- name: Install yum packages
  yum:
    name: '{{ item }}'
    state: present
  with_items:
    - wget
    - git
    - tar

- name: stop firewalld
  service:
    name: firewalld
    state: stopped
    enabled: no
